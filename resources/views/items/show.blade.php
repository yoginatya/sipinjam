@extends('layouts.app') @section('title',$item->name) @section('page-title', __('messages.item_detail')) @section('page-subtitle',__('messages.item_info_availability'))
@section('content')<div class="mx-auto max-w-4xl"><a href="{{route('items.index')}}" class="mb-4 inline-block text-sm font-bold text-blue-600">← {{ __('messages.back') }}</a><div class="overflow-hidden rounded-3xl bg-white shadow-sm"><div class="grid md:grid-cols-2"><div class="flex min-h-[360px] items-center justify-center bg-gradient-to-br from-blue-50 to-slate-100 text-8xl text-slate-400"><i class="fa-solid fa-box"></i></div><div class="p-8"><span class="rounded-full bg-blue-50 px-3 py-1 text-[10px] font-bold text-blue-700">{{$item->category->name}}</span><h1 class="mt-4 text-3xl font-extrabold">{{$item->name}}</h1><p class="mt-1 text-xs text-slate-400">{{ __('messages.code') }} {{$item->code}}</p><div class="my-6 border-t"></div><p class="text-sm leading-7 text-slate-500">{{$item->description}}</p><div class="mt-6 grid grid-cols-2 gap-3"><div class="rounded-2xl bg-slate-50 p-4"><div class="text-xs text-slate-400">{{ __('messages.stock') }}</div><div class="mt-1 text-2xl font-extrabold">{{$item->stock}}</div></div><div class="rounded-2xl bg-blue-50 p-4"><div class="text-xs text-blue-500">{{ __('messages.available') }}</div><div class="mt-1 text-2xl font-extrabold text-blue-600">{{$item->available_stock}}</div></div></div><div class="mt-4 text-xs text-slate-500">{{ __('messages.condition_colon') }} <b class="capitalize">{{ __('messages.condition_'.$item->condition) }}</b></div>@if($item->available_stock>0)<a href="{{route('loans.create',$item)}}" class="mt-7 block rounded-xl bg-[#1b4e8a] py-3.5 text-center text-sm font-bold text-white">{{ __('messages.submit_loan') }}</a>@else<button disabled class="mt-7 w-full rounded-xl bg-slate-200 py-3.5 text-sm font-bold text-slate-400">{{ __('messages.out_of_stock') }}</button>@endif</div></div></div></div>@endsection
